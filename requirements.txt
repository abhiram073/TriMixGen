# Core
numpy>=1.26.0      # Fundamental package for array computing
pandas>=2.1.0      # Data manipulation and EDA

# NLP
transformers>=4.38.0 # State-of-the-art models (mBERT, XLM-R, mT5)
datasets>=2.17.0     # Hugging Face datasets management and fast loading
tokenizers>=0.15.0   # Fast subword tokenization
sentencepiece>=0.2.0 # Required for mT5 tokenizers

# Training
torch>=2.2.0         # Core Deep Learning framework
accelerate>=0.27.0   # Simplifies multi-GPU/MPS/CPU training loops
peft>=0.9.0          # Parameter-Efficient Fine-Tuning (LoRA/QLoRA)
bitsandbytes>=0.42.0 # 4-bit and 8-bit quantization for PEFT
scikit-learn>=1.4.0  # Baselines (CRF) and evaluation metrics

# Visualization
matplotlib>=3.8.0    # Foundational plotting for EDA
seaborn>=0.13.0      # High-level statistical visualizations

# Experiment Tracking
tensorboard>=2.16.0  # Mandatory experiment tracking for loss/metrics
wandb>=0.16.0        # (Optional) Cloud-based experiment tracking

# Deployment
fastapi>=0.110.0     # High-performance REST API
uvicorn>=0.27.0      # ASGI server for FastAPI

# Development & Quality
black>=24.2.0        # Opinionated code formatter
ruff>=0.3.0          # Extremely fast Python linter
isort>=5.13.0        # Import sorting
pre-commit>=3.6.0    # Git hook management

# Testing
pytest>=8.0.0        # Unit and integration testing framework
httpx<0.28.0         # Pinned for FastAPI TestClient compatibility
