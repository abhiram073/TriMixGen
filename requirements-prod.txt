# Production Dependencies ONLY
# Excludes: torch, transformers, datasets (use DEMO_MODE instead)

# Core (lightweight)
numpy>=1.26.0
pandas>=2.1.0
pyyaml>=6.0  # For config files

# API
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.6

# System & Utilities
psutil>=5.9.0  # For system monitoring in API health checks
python-dotenv>=1.0.0  # Environment variable management

# ML & Data Processing (lightweight)
scikit-learn>=1.4.0  # For metrics, baseline classifiers
